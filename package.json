{
  "name": "event-monitor",
  "version": "0.1.0",
  "private": true,
  "description": "X API Event Monitoring & Visualization App",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "worker:batch": "pnpm --filter @event-monitor/worker run batch"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
